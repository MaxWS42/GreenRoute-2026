This is a repo for the GreenRoute sustainable travel project, started for the Sevenoaks Hackathon 2026.

Install requirements and dependencies with:

`pip install -r requirements.txt`

`npm install`


Run these commands in the terminal to test:

`cd my-app`

`npm run dev`
